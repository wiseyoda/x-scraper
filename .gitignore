# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
*.tsbuildinfo

# Test output
coverage/
.vitest-cache/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Logs
*.log
logs/
npm-debug.log*
pnpm-debug.log*

# Local secrets and runtime data
.env
.env.local
.env.*.local

# Spike scratch / profile data / live fixtures that shouldn't ship.
# Fixtures contain real bookmark data and (in *.captured.json) auth headers.
# Scrubbed golden fixtures used by tests live in golden-corpus/, not here.
spikes/tmp/
spikes/x-profile/
spikes/auth/
spikes/fixtures/

# Vault & runtime artifacts (the actual user vault lives in ~/Documents/x-scraper-vault).
# Rooted at repo top so it does not catch packages/vault/.
/vault/
*.kuzu/
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Codex review artifacts (latest few are kept manually)
docs/codex/

# Local scratch dir for ramp logs, snapshots, and codex review run logs.
# Operational artifacts of a single ingest session — not source.
.repostat/
