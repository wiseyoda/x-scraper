{
  "name": "@x-scraper/synthesizer",
  "version": "0.0.0",
  "private": true,
  "description": "L0 → L1 promotion. Cluster claims, draft Ideas via Sonnet, persist with provenance.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch"
  },
  "dependencies": {
    "@x-scraper/core": "workspace:*",
    "@x-scraper/graph": "workspace:*",
    "@x-scraper/llm": "workspace:*",
    "@x-scraper/observability": "workspace:*",
    "@x-scraper/vault": "workspace:*",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
